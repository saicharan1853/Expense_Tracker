frontend
src/
├── api/
├── assets/
├── components/
├── constants/
├── hooks/
├── layouts/
├── modules/
│   ├── auth/
│   ├── dashboard/
│   ├── transactions/
│   └── categories/
├── pages/
├── routes/
├── services/
├── store/
├── styles/
├── types/
└── utils/

backend

src/
├── auth/
├── users/
├── transactions/
├── categories/
├── budgets/
├── reports/
├── common/
│   ├── decorators/
│   ├── filters/
│   ├── guards/
│   ├── interceptors/
│   └── pipes/
├── config/
├── database/
└── main.ts

users
------
id
name
email
password
created_at
updated_at

transactions
------------
id
title
amount
type
note
transaction_date
user_id
category_id
created_at
updated_at

categories
----------
id
name
color
icon
user_id (nullable for default categories)

budgets
-------
id
month
limit
category_id
user_id

auth/
├── auth.module.ts
├── auth.controller.ts
├── auth.service.ts
├── dto/
├── guards/
├── strategies/
└── decorators/

POST /auth/register
POST /auth/login
GET  /auth/profile

GET    /transactions
POST   /transactions
GET    /transactions/:id
PATCH  /transactions/:id
DELETE /transactions/:id

GET    /categories
POST   /categories
PATCH  /categories/:id
DELETE /categories/:id

public frontend 
/login
/register
protected frontend
/dashboard
/transactions
/categories
/reports
/settings

Step 1

Setup PostgreSQL.

Step 2

Create NestJS backend.

Step 3

Implement authentication.

Step 4

Create transaction APIs.

Step 5

Setup React frontend.

Step 6

Connect frontend to backend.

Step 7

Build dashboard UI.

Step 8

Add charts & analytics.

DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=expense_user
DB_PASSWORD=123
DB_DATABASE=expense_tracker